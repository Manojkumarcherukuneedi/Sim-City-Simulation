#ifndef REGION_ANALYSIS_H
#define REGION_ANALYSIS_H
#include "region.h"

void displayFinalStats(const std::vector<std::vector<Cell>> &region);
void analyzeRegion(const std::vector<std::vector<Cell>> &region);

#endif